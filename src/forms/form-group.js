/**
 * FormGroup — aggregates multiple Field instances.
 * Provides form-level state (valid, dirty, touched) as a read-only aggregate.
 * Replaces AngularJS form controller.
 *
 * @module forms/form-group
 */

export class FormGroup {
  // TODO: Phase 6, Task 6.7
  // - addField(name, field) / removeField(name)
  // - Aggregate state: valid, dirty, touched, pending
  // - errors object: { fieldName: { errorKey: true } }
  // - reset(values) — reset all fields
  // - Block submit when invalid + mark all touched
}
