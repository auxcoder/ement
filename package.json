{
  "name": "ement",
  "version": "0.1.0",
  "description": "Modern web framework — Custom Elements + native browser APIs",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/**/*.test.js"
  },
  "devDependencies": {
    "vite": "8.2.1"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  }
}
